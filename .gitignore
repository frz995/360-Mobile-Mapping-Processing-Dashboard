# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example

# TypeScript build info
*.tsbuildinfo

# Remotion render cache
remotion/.remotion/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python
__pycache__/
*.py[cod]
worker/.venv/
worker/_tmp_*.npy

# Cloudflare Pages / Wrangler local dev
.wrangler
.dev.vars

# tsc -b emits these from vite.config.ts (tsconfig.node.json is `composite`
# with no noEmit). vite.config.ts is the single source of truth; Vite reads it
# directly, so the generated copies must never be committed or hand-edited.
vite.config.js
vite.config.d.ts
tsconfig.node.tsbuildinfo

# Local scratch & temporary notes
scratch/
git clone supabase.txt
opencode.json

